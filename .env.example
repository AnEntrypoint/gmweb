# gmweb Docker Compose Configuration
# Copy this file to .env and adjust values as needed

# VNC password for desktop access
VNC_PW=password

# CPU Architecture for base image (aarch64 or x86_64)
# Default: aarch64
# For x86_64 systems, set to: x86_64
ARCH=aarch64
