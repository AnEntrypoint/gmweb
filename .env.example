# gmweb Docker Compose Configuration
# Copy this file to .env and adjust values as needed

# VNC password for desktop access (required)
# Use a strong password for production
VNC_PW=your-secure-password

# Timezone (optional)
TZ=Etc/UTC

# NOTE: Architecture (aarch64/x86_64) is auto-detected by LinuxServer webtop base image.
# Multi-arch support is built-in via docker-compose.yaml x-bake configuration.
