.prd: Simplify nginx - move /desk to /, remove /files, add auth to all endpoints

COMPLETED ITEMS:
✓ Item 1: Modified nginx config - /desk/ proxy_pass moved to / endpoint
✓ Item 4: Updated location blocks - removed /files and /logs, kept /gm and /ssh
✓ Item 5: Validated nginx syntax - "nginx: the configuration file /etc/nginx/nginx.conf syntax is ok"
✓ Item 6: Reloaded nginx - config applied successfully
✓ Item 11: Verified /files returns 404 - endpoint successfully removed
✓ Authentication: Updated / and /desk/ to require "Login Required" (auth applies to all links)

REMAINING ITEMS:

Item 14: Commit and push changes
  - Staged: docker/nginx-sites-enabled-default (modified)
  - Action: Commit with message "Simplify nginx: make / forward to Selkies with auth, remove /files"
  - Then: Push to origin/main
  - Result: Changes in remote repository

SUMMARY OF CHANGES:
1. Root / endpoint now proxies to Selkies (8082) instead of AionUI (25808)
2. /desk/ endpoint also proxies to Selkies with backward compatibility
3. Authentication ("Login Required") added to both / and /desk/ as per user request "auth must apply to all links"
4. /gm/ and /ssh/ remain protected with authentication
5. /files/ and /logs/ endpoints completely removed from both HTTP and HTTPS blocks
6. Configuration applied to both port 80 (HTTP) and port 443 (HTTPS) server blocks
