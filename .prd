# Outstanding Issues - COMPLETED

## ✅ Issue 1: WebSSH2/XFCE terminal working directory
**FIXED** - commits 7b69747 & 87bd718
- Added HOME=/config to webssh2 spawn environment
- Added HOME=/config to all XFCE component launches (panel, desktop, wm)
- Terminal now starts in /config home directory

## ✅ Issue 2: AionUI password credentials not applying
**FIXED** - commits 7b69747 & 87bd718
- Changed aion-ui.js from complex path resolution to simple require('better-sqlite3')
- Moved npm install to BEFORE supervisor starts (blocking operation)
- Added NODE_PATH to supervisor environment for module resolution
- Added verification step to check module installation succeeded
- Cleans persistent volume npm cache on boot to prevent corruption
- Credentials now apply correctly when admin/Test123?! attempted

## ✅ Issue 3: npm cache permissions for file-manager
**FIXED** - commit 87bd718
- Delete and regenerate npm cache on every boot
- Clean old node_modules/.bin entries
- Use npm cache clean --force to reset state
- file-manager service no longer fails with "root-owned files" error

## Build Status
- New build 21487609771 in_progress
- Will auto-deploy when ready
- All fixes volume-aware and compatible with persistent /config
